[project]
name = "dbt-on-snowflake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core==1.12.5",
    "dbt-snowflake==1.12.1",
    "duckdb>=1.5.5",
    "prefect>=3.7.8",
    "prefect-aws>=0.7.9",
    "prefect-dbt>=0.7.25",
]
